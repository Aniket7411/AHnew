import type { ReactNode } from "react";

/**
 * Shared layout (header, footer, etc.). Replace with your React Layout
 * when you share the components folder.
 */
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
